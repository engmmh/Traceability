# These are Supabase Edge Function secrets — set them via
# Dashboard → Edge Functions → inspection-api → Secrets, not in a .env file.
# This file only documents what's needed; never commit real values.

APP_PASSWORD=CHANGE_ME
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET
